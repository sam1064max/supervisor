# Supervisor

Configuration lives here as plain YAML for human inspection. The application
reads environment variables and `.env` via Pydantic Settings; the YAML files
are documented reference examples only.

## Files

- `settings.example.yaml` - documents the supported keys.
- `profiles/` - profile-specific overrides.
